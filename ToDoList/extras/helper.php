<?php

//Use this file to create your tables
//Uncomment the below to execute

/*

$dbServerName=""; //Add your server name or IP here
$dbUsername=""; //Add DB username
$dbPassword=""; //Add your DB password
$dbName=""; //Add your DB name

$dbConnection = mysqli_connect($dbServerName, $dbUsername, $dbPassword, $dbName); //Connection 

  $sqlCreateTableTasks = "CREATE TABLE tasks (
    task_id int(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
    task_text varchar(255) NOT NULL,
    user_id int(11) NOT NULL);";

  mysqli_query($dbConnection, $sqlCreateTableTasks);


  $sqlCreateTableUsers = "CREATE TABLE users (
    user_id int(11) NOT NULL AUTO_INCREMENT PRIMARY KEY,
    user_username varchar(120) NOT NULL,
    user_email varchar(120) NOT NULL,
    user_password varchar(120) NOT NULL);";

  mysqli_query($dbConnection, $sqlCreateTableUsers);

  */

 ?>